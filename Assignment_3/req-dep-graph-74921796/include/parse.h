#ifndef PARSE_H
#define PARSE_H

#include "graph.h"

void parse_requirements(const char *filename, Graph *graph); // If called, parse the requirements from a file and add them to the graph

#endif // PARSE_H